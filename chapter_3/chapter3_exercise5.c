#include <stdio.h>

int main() {
    float x = 0;
    int i = 0;
    float y = 0;

    printf("Enter the values for x, i and y: \n");
    scanf("%f%d%f", &x, &i, &y);

    printf("x value: %f\n", x);
    printf("i value: %d\n", i);

    return 0;
}
